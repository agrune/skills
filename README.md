# agrune-skills

Deprecated.

Agrune no longer distributes agent behavior through this skills repository.
The current integration path is the Agrune MCP server and its built-in tool descriptions.

This repository is archived because the old skills reference removed tools and legacy manifest-authoring flows.
